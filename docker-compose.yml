services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  coturn:
    image: coturn/coturn:4.8.0-alpine
    restart: unless-stopped
    env_file:
      - .env
    command:
      - -n
      - --log-file=stdout
      - --fingerprint
      - --lt-cred-mech
      - --user=${TURN_USER}:${TURN_PASS}
      - --realm=${TURN_REALM}
      - --no-cli
      - --no-tls
      - --no-dtls
      - --min-port=${TURN_MIN_PORT:-50000}
      - --max-port=${TURN_MAX_PORT:-50040}
    ports:
      - "3478:3478/udp"
      - "3478:3478/tcp"
      - "${TURN_MIN_PORT:-50000}-${TURN_MAX_PORT:-50040}:${TURN_MIN_PORT:-50000}-${TURN_MAX_PORT:-50040}/udp"

volumes:
  pgdata:
